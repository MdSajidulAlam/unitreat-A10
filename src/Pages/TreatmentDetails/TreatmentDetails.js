import React from 'react';
import { Button } from 'react-bootstrap';

const TreatmentDetails = () => {
    return (
        <div>
            <Button variant='primary'>Procced CheckOut</Button>
        </div>
    );
};

export default TreatmentDetails;